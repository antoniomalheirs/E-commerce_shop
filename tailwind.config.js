/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
     // Corrected path and added wildcard
  ],
  theme: {
    extend: {
      colors: {
        // You can extend colors here if needed
      },
    },
  },
  plugins: [],
}